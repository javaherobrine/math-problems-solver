#include"matrix_base.hpp"
using namespace std;
void gaussian_elimination(Matrix& equation){//I'm not as smart as Gauss,I'm forbidden to use gaussian elimination,but it's useless
	auto mat=vector<vector<int>>(equation.mat);
}
